// Placeholder so ctest has something to run before the lexer exists.
// Deleting this once there are real tests.

#include <gtest/gtest.h>

TEST(Sanity, CheckAdditionWorks) {
  EXPECT_EQ(2 + 2, 4);
}